# 2026-09-13 验收记录

## 结论

仓库已公开；公开源码不等于服务已部署，也不等于完整验收通过。本轮仍有阻断问题，不建议作为已验收完成版交付。

## 已完成检查

自动化检查共49项通过（8项构建产物/模型流程检查、41项TypeScript回归测试）。独立发布目录的测试配置已改为检查实际存在的Cloudflare绑定配置，不再依赖开发机专属托管配置。

在现有本地学习台切换两个概率论章节树节点，前后视图变换一致，未复现选中后重置；当时页面KaTeX错误节点数为0。这是抽样，不代表全部历史资料没有乱码。

## 学生反馈三轮实测

使用已缓存并核对过的方向导数题目蓝图，模拟“会算梯度但不理解单位化”，随后故意回答“不单位化，方向加倍导数加倍”，最后要求继续。三轮HTTP均为200，分别耗时2.36、16.43、17.99秒；这些不是从上传开始的冷启动耗时。

第一轮确实提出了针对单位化的诊断问题。第二轮能回应误区并正确计算未单位化点积12、单位化结果4。但第三轮仍出现把局部变化率4说成前进一单位实际增加4的不严谨表述。讲解还提前说到第2、3步，而标准答案及检查点仅推进第1步。不能仅凭最终数值正确判为体验通过。

## 改动及后续处理

个性化解释与继续讲解改用解题模型，并增加局部导数与有限增量的区分要求。实测说明仅增加提示词和更强模型不足以消除问题。下一步需要把当前步骤约束与输出结构绑定，并对新增数学解释作独立校验；这两项尚未验收通过。

## 尚未覆盖

Chrome文件上传受扩展权限限制；此前仅验证PDF上传接口成功，完整浏览器上传、PPT解析、大文件、依赖图最新缩放修复及刷新恢复仍需补测。依赖安全告警尚未全部处理。没有进行20名真人盲测，也不宣称零缺陷。密钥、用户课件、数据库及课件截图不在本仓库内。
